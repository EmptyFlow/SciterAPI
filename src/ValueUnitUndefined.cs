namespace SciterLibraryAPI {
    public enum ValueUnitUndefined : uint { // Original name VALUE_UNIT_UNDEFINED
        UT_NOTHING = 1 // T_UNDEFINED && UT_NOTHING -  'nothing' a.k.a. 'void' value in script 
    };

}
