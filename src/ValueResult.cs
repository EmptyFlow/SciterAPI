namespace SciterLibraryAPI {
    public enum ValueResult : int { // Original name VALUE_RESULT
        HV_OK_TRUE = -1,
        HV_OK = 0,
        HV_BAD_PARAMETER = 1,
        HV_INCOMPATIBLE_TYPE = 2
    }

}
